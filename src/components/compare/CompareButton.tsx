import { CompareIcon } from "../icons/СompareIcon";
import { FC } from "react";

const CompareButton: FC = () => {
  return (
    <button className={"header-action"}>
      <CompareIcon />
    </button>
  );
};
export default CompareButton;
